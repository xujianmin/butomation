<% content_for :title, "New lottery" %>

<div class="row">
  <div class="col-12">
    <div class="align-items-center p-3 my-3 bg-primary-subtle rounded shadow-sm clearfix">
      <p class="h5 mb-0 float-start text-primary-emphasis">新建抽签计划</p>
      <%= link_to lotteries_path, class: "float-end btn btn-sm btn-primary" do %>
        <i class="bi bi-plus"></i>
        抽签计划一览
      <% end %>
    </div>

    <%= render "form", lottery: @lottery %>
  </div>
</div>