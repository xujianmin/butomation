<% breadcrumb :new_user %>

<% content_for :title, "新建用户" %>

<div class="row mt-3">
  <div class="col-12">
    <div class="card">
      <div class="card-header">
        <h3>新建用户</h3>
      </div>
      <div class="card-body">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div> 